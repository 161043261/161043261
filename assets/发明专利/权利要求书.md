1.一种基于 BBR v1 拥塞控制协议的动态参数优化系统，包括发送端和接收端，其特征在于：发送端包括强化学习模型和网络协议栈，所述强化学习模型可以根据发送端网络协议栈实时采集链路状态特征，动态生成对 BBR 关键参数，包括拥塞窗口增益、最小 RTT 探测周期的调整策略。所述网络协议栈包括 BBR 状态机，该 BBR 状态机根据所述调整策略，优化 BBR 关键参数。接收端包括拥塞信息反馈模块，接收端收到 ECN 数据包时，将拥塞信息反馈给发送端，进而对发送端的拥塞窗口增益、最小 RTT 探测周期进行细粒度调整，同时作为发送端的所述强化学习模型的输入之一，协助生成所述参数调整策略。2.根据权利要求 1 所述的系统，其特征在于：所述接收端的拥塞信息反馈模块，接收端收到携带 CE 拥塞经 w 历标记的数据包后，将拥塞信息，即携带 ECN-Echo 标记的 ACK 反馈给发送端。3.根据权利要求 1 所述的系统，其特征在于：发送端收到拥塞信息，即携带 ECN-Echo 标记的 ACK 后，使用该拥塞信息对拥塞窗口增益、最小 RTT 探测周期进行细粒度调整，细粒度调整的公式为：

4.根据据权利要求 1 所述的系统，其特征在于：发送端会持续维护指数加权移动平均的 RTT 抖动，用于响应链路状态的突发改变，指数加权移动平均的 RTT 抖动的计算公式为：

5.根据权利要求 1 所述的系统，其特征在于：所述强化学习模型采用 PPO 近端策略优化的强化学习算法，结合离线学习和在线学习对模型进行训练和更新，更新过程用公式表示为：

6.根据权利要求 1 所述的系统，其特征在于：所述强化学习模型可以适用于不同的网络环境，例如数据中心网络或无线网络。通过实时采集链路特征，动态优化 BBR 关键参数，保证在不同网络环境下提高网络传输性能。7.一种对 BBR 关键参数的动态优化方法，其特征在于：该方法分为以下步骤：
步骤 1、发送端和接收端运行 BBR 协议，发送端运行前可以加载对应网络环境下的预训练参数，也可以使用默认参数；
步骤 2、运行时发送端网络协议栈中的强化学习模型根据实时采集的链路状态特征，动态生成对 BBR 关键参数，包括拥塞窗口增益、最小 RTT 探测周期的调整策略；
步骤 3、将步骤 2 中生成的调整策略应用于 BBR 协议，更新 BBR 关键参数；
步骤 4、接收端收到携带 CE 拥塞经历标记的数据包后，将拥塞信息，即携带 ECN-Echo 标记的 ACK 反馈给发送端；
步骤 5、发送端收到携带 ECN-Echo 标记的 ACK 后，细粒度调整拥塞窗口增益、最小 RTT 探测周期；同时作为发送端的所述强化学习模型的输入之一，协助生成所述参数调整策略。
步骤 6、发送端使用优化后的 BBR 关键参数，继续发送后续数据流。8.根据权利要求 7 所述的，对 BBR 关键参数的动态优化方法，其特征在于：步骤 1 中实时采集的链路状态特征，包括链路瓶颈带宽和最小 RTT、当前的交付速率、近期的 RTT 抖动和丢包率等，以及所述接收端反馈的拥塞信息，一并作为所述发送端的强化学习模型的输入，以动态生成所述参数调整策略。

====================================================================================================

1. 一种基于客户端主动探测的拥塞控制改进方法，其特征在于，包括以下步骤：
   步骤 1、客户端测量与计算：客户端启动一个探测模块，该探测模块周期性的发送探测包并接收响应包，以实时测量链路的往返时延。基于所述探测模块测量得到的一组 RTT 值，计算反映近期链路拥塞程度的往返时延抖动（RTT Jitter）指标。
   步骤 2、客户端反馈信号生成：根据步骤 1 计算的往返时延抖动指标，对等待返回给服务器的 ACK 包中，IP 头部的两个保留比特位进行编码，以生成携带客户端探测链路拥塞程度的反馈信号。
   步骤 3、服务器发送速率调整：服务器端接收并解析所述携带客户端探测的拥塞信号的 ACK 包，根据所述 ACK 包 IP 头部的两个保留比特位的编码值，结合服务器自身的拥塞控制算法逻辑和显式拥塞通知算法，对发送速率进行实时调整
2. 根据权利要求 1 所述的方法，其特征在于，所述步骤 2 中，往返时延抖动指标的计算具体包括以下步骤：
   步骤 1、在客户端维护一个具有预设容量的 RTT 样本缓冲队列，缓存所述探测模块近期测量的一组 RTT 值
   步骤 2、基于所述 RTT 样本缓冲队列中的一组 RTT 值，平滑处理后得到一个反映近期 RTT 抖动程度的平滑值
3. 根据权利要求 1 所述的方法，其特征在于，所述步骤 2 中，对 ACK 包 IP 头部的两个保留比特位进行编码的具体逻辑是：将步骤 1 计算的 RTT 抖动指标与该链路预设的抖动阈值上限进行对比。如果所述 RTT 抖动指标大于所述抖动阈值上限，则判断链路的拥塞风险增加，并将 ACK 包 IP 头部的两个保留比特位编码为 01；如果所述 RTT 抖动指标小于等于所述抖动阈值下限，则判定链路状态稳定，并将 ACK 包 IP 头部的两个保留比特位编码为 10，00 和 11 编码保留原显式拥塞通知语义
4. 根据权利要求 1 和 3 所述的方法，其特征在于，服务器端根据 ACK 包 IP 头部的两个保留比特位标记，调整发送速率的逻辑为：如果收到的 ACK 包中，IP 头部的两个比特位编码为 01，且服务器端当前拥塞窗口大于预设的拥塞窗口大小下限，则减小拥塞窗口（CWND）；如果收到的 ACK 包中，IP 头部的两个比特位编码为 10，且服务器端当前拥塞窗口小于预设的拥塞窗口大小上限，则增大拥塞窗口
5. 根据权利要求 1 至 4 任意一项所述的方法，其特征在于，所述方法兼容显式拥塞通知算法，具体表现在：所述 ACK 包 IP 头部的两个保留比特位直接使用 ECN 字段。当传输路径上的路由器或交换机中，数据包队列堆积程度超过一定阈值，也即检测到拥塞时，标记该数据包，表示拥塞经历（Congestion Experienced, CE），客户端收到携带 CE 标记的数据包后，将确认该数据包的 ACK 包的 ECN 字段编码为 11，表示拥塞回显（ECN-Echo, ECE），以向服务器回显传输路径上的网络设备检测到的拥塞信号，服务器根据 ECN 算法逻辑降低发送速率；当传输路径上的路由器或交换机未检测到拥塞时，ECN 字段编码保持为 00（Not-ECT），不会与本发明提出的编码逻辑冲突
6. 一种基于客户端主动探测的拥塞控制改进系统，其特征在于，该系统包括

- 一个客户端设备，所述客户端设备配置有
  - 探测模块，用于实时测量链路的往返时延（RTT）
  - 计算模块，用于根据所述探测模块测量的多个 RTT 值，计算反映近期链路拥塞程度的 RTT 抖动指标
  - 标记模块，用于根据所述 RTT 抖动指标，对发送到服务器的 ACK 包 IP 头部的 ECN 字段进行标记
- 一个服务器设备，所述服务器设备配置有
  - 解析模块，用于接收并解析所述客户端发送的 ACK 包头部的 ECN 字段值
  - 发送速率调整模块，用于根据所述 ACK 包头部的 ECN 字段值，增大或减小拥塞窗口，以调整发送速率

7. 根据权利要去 6 所述的系统，其特征在于，所述客户端设备的计算模块包括：

- 维护一个具有预设容量的 RTT 样本缓冲队列，采用先进先出（FIFO）策略更新缓存的 RTT 样本
- 采用指数加权平均（EWMA）算法，平滑处理所述缓冲队列中的 RTT 样本，以计算反映近期 RTT 抖动程度的平滑值，作为所述 RTT 抖动指标
