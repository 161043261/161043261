{
  "scripts": {
    "git:add": "git add -A",
    "git:commit": "git commit -m 'feat: Introduce new features'",
    "git:push": "pnpm git:add && pnpm git:commit && pnpm git:push",
    "git:push:f": "pnpm git:add && pnpm git:commit && pnpm git:push:f"
  }
}
